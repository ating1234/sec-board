# 複製此檔案為 .env 並填入實際設定
# cp .env.example .env

# PostgreSQL 連線字串
# 格式：postgresql://使用者:密碼@主機:埠/資料庫名稱
DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/cybersec_db

# 首次啟動用的管理員密碼（至少 8 碼）。啟動後會寫入資料庫，之後可從環境變數移除。
# 若資料庫已有密碼紀錄，這個值會被忽略。
INITIAL_ADMIN_PASSWORD=change_me_on_first_boot
